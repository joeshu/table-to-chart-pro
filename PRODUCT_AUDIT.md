# 表格转图表 Pro：产品、交互、视觉与工程全面审计

## 一、结论

当前版本已经具备可演示的 MVP：粘贴数据、编辑、推荐图表、8 类图表、主题、规则分析、PNG 导出。但它本质上仍是“单页 Demo”，还没有达到真正 Pro 桌面工具的可靠性、效率和完成度。

核心升级方向不是继续堆图表类型，而是形成以下闭环：

> 导入数据 → 自动清洗 → 即时预览 → 精细配置 → 保存项目 → 高质量导出

产品应定位为“本地优先、无需登录、数据不上传的轻量图表工作台”。

## 二、当前优点

- 四步流程清晰，新用户容易理解。
- 支持 Excel/WPS/Numbers 复制粘贴。
- 已包含柱状、折线、饼图、环形、雷达、散点、热力、漏斗。
- 六套主题具备基础一致性。
- 编辑后可直接生成，学习成本低。
- Chart.js 已本地化，具备离线运行基础。
- Tauri 体积和性能方向正确。

## 三、当前关键问题

### P0：可靠性

1. CSV 解析仅按逗号切分，不支持引号、字段内逗号、换行、分号和 BOM。
2. 所有非法数字被静默转成 0，会制造错误图表和错误结论。
3. 行列不一致的数据直接丢弃，但界面没有告知丢弃了哪些行。
4. 比例、增长率、相关系数遇到 0 或常量列会产生 Infinity/NaN。
5. 热力图所有值相同时存在除零问题。
6. 漏斗图首项为 0 时百分比异常；负数也没有校验。
7. “AI 智能分析”实际是本地规则统计，命名会造成误导。
8. SVG 只支持漏斗图，其余图表按钮仍显示，预期不一致。
9. 浏览器下载与剪贴板 API 在 Tauri/iOS 上的行为不稳定，缺少原生保存兜底。
10. 当前项目没有真正调用 Rust，仍是网页套壳。

### P1：使用效率

1. 四步向导适合首次使用，不适合高频办公；每次修改都要前后跳转。
2. 没有实时预览，用户无法边调边看。
3. 不能导入 CSV/XLSX 文件，也不能拖拽文件。
4. 表格仅能删除末行，不能删除指定行/列、插入、排序、批量粘贴。
5. 第一列不可编辑，与“点击任意单元格编辑”的提示冲突。
6. 没有撤销/重做、快捷键、自动保存、最近项目。
7. 没有图表标题、副标题、单位、数据来源、备注配置。
8. 没有横纵轴、图例、数据标签、网格线、数值格式配置。
9. 不能交换行列、指定分类列/X/Y 轴、选择参与绘图的系列。
10. 没有导出尺寸、倍率、透明背景、白色背景、文件名配置。

### P1：视觉与交互

1. 首页顶部信息较大，主工作区下沉；桌面软件更适合“顶部工具栏 + 左数据 + 中画布 + 右属性”。
2. 大量 Emoji 风格不统一，在不同系统显示差异明显，专业感不足。
3. 当前卡片和按钮层级接近，主操作不够聚焦。
4. 页面最大宽度 1100px，没有利用桌面宽屏。
5. 深色主题只作用于图表，不是应用级深色模式。
6. 主题卡片固定配色，缺少自定义颜色与品牌色。
7. 缺少空状态、拖拽状态、解析错误状态、保存成功状态、构建中状态。
8. Toast 是唯一反馈方式，错误信息短暂消失，不利于修正数据。
9. 移动端只是 CSS 压缩，不是针对 iPhone 的触控与布局设计。
10. 可访问性不足：缺少 aria、键盘焦点、减少动画、高对比度适配。

### P1：工程

1. 约 1400 行 HTML/CSS/JS 单文件，维护和测试困难。
2. 大量内联 onclick 和 innerHTML，不利于 CSP、安全审查和模块化。
3. 状态散落在全局变量中，没有统一数据模型。
4. 缺少 TypeScript、Lint、格式化、单元测试、E2E 测试。
5. 没有错误边界与日志记录。
6. 没有项目文件格式和向后兼容策略。
7. Windows 与 iOS 共用同一交互实现，没有平台能力适配层。
8. Tauri 权限、原生文件对话框、文件系统和剪贴板插件尚未接入。

## 四、推荐的新产品结构

### 桌面端工作台

```text
┌ 顶部栏：新建｜打开｜保存｜撤销｜重做｜导出 ┐
├ 左侧 34%：数据区 ┬ 中间 46%：实时图表画布 ┬ 右侧 20%：属性面板 ┤
│ 导入/粘贴/表格   │ 缩放、适配、画布背景     │ 图表/标题/坐标/配色 │
└ 底部状态栏：行列数｜错误数｜本地保存状态｜缩放比例 ┘
```

将四步向导保留为“新手模式”，默认进入高效工作台。所有调整实时反映到画布。

### iPhone/iPad

- iPhone：底部三标签“数据 / 图表 / 样式”，导出为顶部主按钮。
- iPad：双栏布局，左侧数据，右侧画布与抽屉式属性。
- 触控目标至少 44px，避免依赖 hover 和 contenteditable。

## 五、功能升级清单

### 数据导入与清洗

- CSV/TSV/TXT 导入。
- XLSX 导入（前端 SheetJS 或 Rust calamine，优先 Rust）。
- 文件拖拽和系统“打开方式”。
- 自动识别分隔符、编码、表头、数字、百分比、货币、日期。
- 数据质量面板：空值、非法数字、重复行、异常行。
- 清洗选项：忽略、填 0、均值填充、删除行。
- 行列转置、排序、筛选、选择系列。
- 粘贴后立即预览，不要求额外点击识别。

### 图表与设计

第一阶段保留 8 类，不急于继续加类型；先补齐配置：

- 标题、副标题、脚注、数据来源。
- 轴标题、范围、刻度、单位、千分位、小数位。
- 图例位置、数据标签、网格线、动画开关。
- 柱状图：横向、堆叠、百分比堆叠、圆角。
- 折线图：平滑、面积、点样式、缺失值连接。
- 饼图：起始角度、标签、中心文案、合并小项。
- 散点图：趋势线、R²、气泡大小。
- 自定义颜色、品牌色、色盲友好配色。
- 图层背景、边距、安全区和常用尺寸预设。

后续再增加：面积图、组合图、气泡图、瀑布图、甘特图。

### 项目与效率

- `.t2c` JSON 项目格式。
- 新建、打开、保存、另存为。
- 自动保存与崩溃恢复。
- 最近项目列表。
- 撤销/重做历史。
- 模板库：销售趋势、满意度、预算执行、转化漏斗等。
- 快捷键：Cmd/Ctrl+O、S、Z、Shift+Z、E、V。

### 导出

- PNG：1x/2x/3x、透明/白底、自定义尺寸。
- SVG：为全部矢量图提供真正 SVG 渲染；Chart.js Canvas 无法直接得到原生 SVG，应接入 SVG 渲染器或明确限制。
- PDF：适合打印和汇报。
- CSV：导出清洗后的数据。
- 复制图片、复制 SVG、复制分析文案。
- 常用尺寸：16:9 PPT、4:3、A4、公众号头图、自定义。

### 数据洞察

将“AI 智能分析”改为“本地数据洞察”，并明确“数据不离开设备”。

基础版：
- 总计、均值、中位数、最大最小、同比环比。
- 异常点、趋势、波动率、相关系数、漏斗转化。
- 对除零、样本不足、常量列输出可理解提示。

可选 AI：
- 用户主动启用并配置模型后才发送数据。
- 发送前预览和脱敏。
- 严格区分“本地统计结果”和“模型生成解读”。

## 六、视觉升级规范

### 风格定位

“专业、克制、轻量”，避免“神器 + 大量 Emoji”的网页工具感。

推荐品牌：
- 名称：表格转图表 Pro
- 主色：Indigo `#5B5CE2`
- 强调色：Cyan `#18A7C9`
- 背景：`#F6F7FB`
- 正文：`#172033`
- 次级文字：`#697386`
- 边框：`#E5E8F0`
- 成功：`#168A62`
- 警告：`#C87A10`
- 危险：`#D64545`

### 组件规范

- 8px 间距系统；卡片圆角 12px，按钮 9px。
- 使用统一 SVG 图标，不使用系统 Emoji 作为核心图标。
- 主按钮每个页面最多一个。
- 输入错误常驻展示在字段旁，不只用 Toast。
- 支持系统深色模式。
- 使用 13/14/16/20/28px 字号阶梯。
- 动画 150–220ms，并尊重 `prefers-reduced-motion`。

## 七、工程改造方案

建议目录：

```text
src/
  app.ts
  styles/
  components/
  charts/
  data/
    parser.ts
    validator.ts
    statistics.ts
  platform/
    files.ts
    clipboard.ts
  state/
  types/
src-tauri/
  src/
    lib.rs
    commands/
      files.rs
      export.rs
      project.rs
```

技术选择：

- TypeScript + Vite。
- UI 可用原生组件化或轻量框架；建议 Vue 3，学习和维护成本适中。
- Rust 使用 `csv`、`calamine`、`serde` 负责文件解析和项目存储。
- Tauri 插件：dialog、fs、clipboard-manager、store、log、opener。
- Vitest 测试解析、统计和配置迁移。
- Playwright 测试“导入 → 编辑 → 生成 → 导出”主链路。
- Actions 增加 `npm test`、`cargo check` 后再打包。

## 八、实施顺序

### V1.1：先把产品做稳

1. 修复 CSV/数值/除零/异常行问题。
2. “AI”改名为“本地数据洞察”。
3. 增加错误面板和数据质量提示。
4. 增加标题、单位、图例、数据标签、导出倍率。
5. 支持指定行列删除、第一列编辑、撤销重做。
6. 拆分 HTML/CSS/JS，加入 TypeScript/Vite 与测试。

### V1.2：变成真正桌面应用

1. CSV/XLSX 文件导入。
2. Tauri 原生打开、保存、剪贴板、最近项目。
3. `.t2c` 项目保存与自动恢复。
4. 工作台三栏布局、实时预览、深色模式。
5. PNG/PDF/CSV 完整导出。

### V1.3：专业能力

1. 组合图、堆叠、趋势线、色盲主题。
2. 模板库和品牌主题。
3. 可选 AI 解读。
4. 签名的 Windows 发布和 iOS 真机分发。

## 九、验收指标

- 10 秒内从 Excel 粘贴并生成第一张图。
- 修改数据后 100ms 内刷新预览（中小数据集）。
- 非法数据不会静默变成 0。
- 所有统计结果不出现 NaN/Infinity。
- 1000 行数据保持可操作。
- Windows 离线可完整导入、保存、导出。
- 核心流程全键盘可用。
- 亮/暗主题达到 WCAG AA 基础对比度。
- 项目异常退出后可恢复最近编辑内容。
