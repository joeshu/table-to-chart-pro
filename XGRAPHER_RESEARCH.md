# xGrapher 对标研究与项目改进建议

研究日期：2026-08-13
研究范围：`/zh-CN`、图表目录、组合图、图表诊断页、公开 Next.js 分包脚本、Recharts 共享组件、保存与导出相关脚本。

## 1. 技术与产品结构

xGrapher 使用 Next.js + React + Recharts。组合图页面脚本约 46KB，Recharts 共享分包约 257KB；压缩后分别约 12KB 和 79KB。编辑器状态由页面组件和共享图表组件组合，浏览器草稿使用 `localStorage`，另有 `/api/charts/` 云端保存能力。

网站不是按“全部图表平铺”组织，而是按用户目的分组：比较、趋势、占比、分布统计、层级关系、流程过程、图形空间、QC、其他。每类提供真实缩略图、用途描述和直接创建入口。图表诊断通过多步问题先判断数值、结构关系或 QC，再按比较、趋势、占比、分布等目的推荐图形。

组合图脚本支持 bar/line/area 混合、y/y2/y3 三轴、系列独立类型/轴/颜色/透明度/线宽/标签位置、堆叠、曲线、点、柱间距、圆角、轴标签旋转/隐藏/紧凑、四边独立 margin。系列使用稳定 UUID，而不是表格列序号。

## 2. 最值得借鉴

### P0：数据驱动的图表推荐

当前项目选择器只列图标，没有告诉用户为什么选。应在解析表格后生成 2–4 个建议，并说明原因：

- 时间/日期分类 + 数值：折线图、面积图。
- 分类较少 + 单系列：柱状图；合计接近 100% 时补饼图建议。
- 多系列且量纲差异明显或包含百分比：组合图并自动分配右轴。
- 正负增减序列：瀑布图。
- 流程阶段且数值大体递减：漏斗图。
- 两个/三个数值列：散点图/气泡图。

建议放在图表类型分组顶部，用户可一键应用。不要照搬独立诊断网页，桌面工具更适合在数据导入后直接给建议。

### P0：用途式图表图库

选择器应增加“推荐、比较、趋势、占比、分布、关系、流程、全部”筛选，卡片使用真实数据缩略图，显示一句适用场景。保留搜索与收藏。现有图标网格适合快速操作，但不利于非专业用户做正确选择。

### P0：稳定系列 ID

当前系列设置以列号 `1/2/3` 为键。插入、删除、拖动列后，别名、颜色、轴和趋势线可能套到错误系列。应给数据列增加稳定 `id`，系列设置按 ID 保存，表头只是显示名称。这是比新增图表类型更优先的正确性改造。

### P1：画布元素直达属性

xGrapher 图表元素带选中状态。项目应支持点击标题、图例、坐标轴、柱、线后自动跳到对应属性页并定位分组。保留当前四页信息架构，不应回退为自由浮动面板。

### P1：第三数值轴

xGrapher 组合图支持 `y/y2/y3`。本项目已有左右双轴，第三轴应只在三个不同量纲确有必要时开放，并加入明显警告，避免图表难读。系列轴设置应从二选一改为轴列表。

### P1：SVG 导出

xGrapher 基于 Recharts/SVG，SVG 是天然优势。本项目基于 Chart.js Canvas，可借助 Chart.js SVG 不现实，优先方案是增加“矢量 PDF”或建立独立 SVG 渲染器。不要伪造 SVG：把 PNG 包进 SVG 没有矢量价值。

### P1：四边独立边距

当前项目只有统一绘图区边距。应支持上、右、下、左分别设置，并提供“自动/紧凑/演示文稿”预设，解决长轴标签、右轴标题和图例挤压。

### P1：自动保存可见化

项目已有草稿和撤销状态，但用户感知较弱。增加“已保存/保存中/未保存”状态、恢复草稿入口和最近自动保存时间。桌面端继续使用项目文件和本地草稿，优于仅依赖 `localStorage`。

### P1：复杂度预算

应设置软限制并解释原因：系列数、数据点数、导出总像素、趋势线计算量。超过建议值时不直接失败，而是建议隐藏系列、减少标签、关闭动画或聚合数据。

## 3. 不应照搬

- 不改用 Next.js：项目是 Tauri 桌面/iOS 工具，Vue 架构更贴合现状。
- 不因 SVG 优势立即替换 Chart.js/Recharts：现有配置、插件和测试迁移成本过高。
- 不复制营销长页和每图一页 SEO 结构：桌面应用应直接进入工作台。
- 不默认开放三轴：双轴已经需要谨慎使用，三轴只作为高级能力。
- 不照搬纯浏览器临时保存：本项目的 `.t2c`、Files、最近项目和原生保存更可靠。
- 不复制源码或文案，只借鉴产品模式和通用交互。

## 4. 推荐实施顺序

1. 稳定列 ID 与系列设置迁移。
2. 数据分析器与图表推荐引擎。
3. 用途式图表选择器和真实缩略图。
4. 点击画布元素定位属性。
5. 四边独立边距与布局预设。
6. 自动保存状态与草稿恢复界面。
7. 性能复杂度预算和降级建议。
8. 调研真正的矢量导出路径。
9. 评估第三数值轴。

## 5. 衡量标准

- 用户导入数据后 2 次点击内得到合理图表。
- 插删和调整数据列后系列样式不串列。
- 组合图默认轴分配正确率可被测试覆盖。
- 常用设置 1 屏可达，高级设置不干扰首次操作。
- 预览、PNG、PDF、项目重开保持一致。
- 大数据时给出可行动的优化建议，而不是空白或崩溃。
